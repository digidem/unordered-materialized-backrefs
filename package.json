{
  "name": "unordered-materialized-backrefs",
  "version": "1.0.0",
  "description": "materialized view to calculate back-references for unordered log messages",
  "devDependencies": {
    "level": "^3.0.0",
    "memdb": "^1.3.1",
    "tape": "^4.9.0"
  },
  "scripts": {
    "test": "tape test/*.js"
  },
  "license": "BSD"
}
